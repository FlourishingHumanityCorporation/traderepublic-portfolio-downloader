schema = 1
project = "traderepublic-portfolio-downloader"

[[surface]]
id = "generate-rest-client"
path = "scripts/generate-rest-client.sh"
purpose = "Regenerate the repository REST client from its source API contract during development."
owner = "traderepublic-portfolio-downloader"
lifecycle = "maintenance"
promotion = "keep-local"

[[surface]]
id = "container-entrypoint"
path = "entrypoint.sh"
purpose = "Install container build prerequisites, vendor modules, run the project gate, and assemble cross-platform binaries inside the release container."
owner = "traderepublic-portfolio-downloader"
lifecycle = "release"
promotion = "external-image"
